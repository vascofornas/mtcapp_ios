export const ENV = {
  PRODUCTION : false,
  API_URL    : 'http://trescantos.proyectos.buscandoquehacer.com',
  GOOGLE_API_KEY : ''
};

/* Este archivo ha sido removido del historial de git para cambios futuros,
eso quiere decir, que la clave de google maps se encuentra protegida */
//git update-index --assume-unchanged ./src/config/environment.ts
//git update-index --no-assume-unchanged ./src/config/environment.ts

/*
export const ENV = {
  PRODUCTION : true,
  API_URL    : 'http://localhost',
  GOOGLE_API_KEY: ''
};
**/
